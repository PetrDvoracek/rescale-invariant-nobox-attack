# Curated direct dependency pins for the rescale-invariant no-box attack repo.
#
# Versions are derived from docker/requirements.lock (the full pip freeze of the
# working, checkpoint-producing environment), NOT from a fresh resolve. See
# docker/README.md.
#
# !! FRAGILE PINS — re-verify on ANY bump (PROGRAM.md §11) !!
#   - albumentations : augmentation defaults / deprecated ops move the F_Q label
#                      and thus attack strength. Number-moving.
#   - timm           : model-name strings, *.in1k tags, and pretrained cfg
#                      (crop_pct/interpolation) move the reported accuracies.
#                      Number-moving.
# Bumping either of the above requires re-running the augment-construction and
# victim-resolution CI checks and confirming the reproduced numbers.
#
# torch / torchvision: pinned to the public release matching the NVIDIA
# pytorch:24.01-py3 base image (lock records the vendored 2.2.0a0 NVIDIA build at
# torch @ file:///... and torchvision @ file:///opt/pytorch/vision). On the
# docker base these come preinstalled; the pins below are the public equivalents
# for non-NGC environments / CI.

torch==2.2.0
torchvision==0.17.0
timm==0.9.2
segmentation-models-pytorch==0.3.3
lightning==2.2.0.post0
albumentations==1.3.1
torchmetrics==1.9.0
opencv-python==4.8.0.74
scikit-image==0.25.2
typer==0.9.0
wandb==0.16.3
click==8.1.7
tqdm==4.65.2
matplotlib==3.8.2
numpy==1.24.4
